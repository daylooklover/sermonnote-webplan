{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      // @/ 경로가 루트에 있는 파일도 찾을 수 있도록 설정
      "@/*": ["./src/*", "./*"], // 이 부분을 수정합니다.
      "@components/*": ["./components/*"] // 이 설정은 그대로 둡니다.
    }
  }
}