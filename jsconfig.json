{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      // 기존 @/* 별칭이 이미 src/*를 가리키므로, 
      // @/components/*를 따로 정의할 필요는 없으나, 명시적으로 하려면 이렇게 합니다.
      "@/components/*": ["./src/components/*"], 
      // 기존 "@components/*"는 삭제하거나 @/*에 흡수됩니다.
      "@/*": ["./src/*"] 
    }
  },
  "include": ["src/**/*"]
}